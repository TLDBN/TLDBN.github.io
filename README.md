# Bonan Ding Academic Homepage

This is a dependency-free static website for GitHub Pages. Publish the contents of this directory at the repository root on the `main` branch.

The only third-party runtime resource is the Statable visitor-map widget configured in `index.html`. All other presentation assets are local under `assets/`.
